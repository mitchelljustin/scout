module util
    fn range(start, ending) = do
        array = []
        i = start
        while i < ending
            array = array + [i]
            i = i + 1
        end
        array
    end
end

loop
    std::print("enter> ")
    input = std::readline()
    if input == "exit"
        break
    end
    result = std::num(input)
    std::println(result + result)
end
