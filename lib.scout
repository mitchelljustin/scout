module util
    fn range(start, ending) = do
        array = []
        i = start
        while i < ending
            array = array + [i]
            i = i + 1
        end
        array
    end

    fn self.swag() = 0
end

class Test
    fn test() = 0
end
