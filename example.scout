
mod mitch
    fn append_mitch(str) = do
        mitch = " mitch"
        std::str(str, mitch)
    end
end
fn main() = do
    z = 1 + 3 + 4 - 7
    x = std::str(z, true, "string")
    std::print(mitch::append_mitch(x))
end
main()
